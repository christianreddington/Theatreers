variables:
  service: Theatreers.Admin

  paths:
    include:
    - /src/${{ service }}/*
    - /pipelines/${{ service }}*

stages:
- template: /pipelines/stages/lifecycle.yml
  parameters:
    service: ${{ service }}