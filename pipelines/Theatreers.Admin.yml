variables:
  service: Theatreers.Admin

trigger:
  paths:
    include:
    - /src/${{ variables.service }}/*
    - /pipelines/${{ variables.service }}/*

stages:
- template: /pipelines/stages/lifecycle.yml
  parameters:
    service: ${{ variables.service }}